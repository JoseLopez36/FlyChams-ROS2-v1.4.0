/flychams/**:
  ros__parameters:
    # GUI manager parameters
    gui_manager:
      update_rate: 20.0 # (Hz)
    # Metrics factory parameters
    metrics_factory:
      update_rate: 10.0 # (Hz)
    # Marker factory parameters
    marker_factory:
      update_rate: 10.0 # (Hz)